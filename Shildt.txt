Глава 17 стр 498 Пакет java.lang

К лекции 6
- почему не компилируется Consumer<Integer> consumer1 = (int i) -> {};
- захват перременных в лямбда выражении (действительно завершенные переменные)
- в лямбда выражении можно изменять переменную экземпляра вызывающего его класса,
    но нельзя изменять локальную переменную из из объемлющей области действия
- ссылки на методы экземпляра (разобраться!!!)
- ссылка на метод экземпляра работает точно также как и на стратический метод ?
- ссылка на метод класса (не статический метод), разобраться !!!

Stream API
- разобраться как писать собственные коллекторы
- операции с сохраннием состояния и без сохранения состояния
- DoubleStream mapToDouble(ToDoubeFunction<? super Т> функция_отображения)
    попросить объяснить значение "? super Т"

    Skype: vladimir.ivanov_am (Send message) (Call)
    Email: vladimir.ivanov@digital-ecosystems.ru (Send email)
    Телефон: +79137834791 (Call)

    Иванов Владимир Александрович

    Отдел: Информационно-аналитический отдел
    Должность: Начальник информационно-аналитического отдела
    Статус: Chief